# راهنمای بسیار ساده انتشار سایت دائمی تولید نوین

این نسخه برای دسترسی از هر مکان با اینترنت آماده شده است. سایت دارای ورود مدیر و کارمند و دیتابیس مرکزی است.

## آنچه نیاز دارید

1. یک حساب GitHub
2. یک حساب Render
3. یک رمز قوی برای مدیر
4. سرویس دارای Persistent Disk؛ برای نگهداری دائمی دیتابیس

> سرویس رایگان بدون دیسک دائمی برای اطلاعات کارخانه مناسب نیست؛ با خاموش یا بازسازی‌شدن سرویس ممکن است داده‌ها پاک شوند. برای استفاده واقعی، پلن دارای دیسک دائمی را انتخاب کنید.

## مرحله اول: ساخت حساب GitHub

1. به `https://github.com` بروید.
2. حساب بسازید و ایمیل را تأیید کنید.
3. از بالای صفحه روی New repository بزنید.
4. نام مخزن را `novin-production` بگذارید.
5. مخزن را Private انتخاب کنید.
6. روی Create repository بزنید.

## مرحله دوم: بارگذاری فایل‌ها

1. فایل `novin-production-cloud.zip` را از حالت فشرده خارج کنید.
2. داخل مخزن GitHub گزینه `uploading an existing file` یا `Add file → Upload files` را بزنید.
3. تمام فایل‌های داخل پوشه استخراج‌شده را بارگذاری کنید؛ خود پوشه ZIP را بارگذاری نکنید.
4. روی Commit changes بزنید.

فایل‌های مهم باید در ریشه مخزن دیده شوند:

```text
server.py
render.yaml
Dockerfile
requirements.txt
static/index.html
```

## مرحله سوم: ساخت حساب Render

1. به `https://render.com` بروید.
2. با حساب GitHub وارد شوید.
3. از Dashboard گزینه New را بزنید.
4. Blueprint را انتخاب کنید.
5. مخزن `novin-production` را متصل کنید.
6. Render فایل `render.yaml` را شناسایی می‌کند.

## مرحله چهارم: تعیین رمز مدیر

در زمان ساخت Blueprint، مقدار متغیر زیر درخواست می‌شود:

```text
NOVIN_ADMIN_PASSWORD
```

یک رمز قوی حداقل ۱۲ کاراکتری وارد کنید. نمونه ساختار مناسب:

```text
Novin-Factory-1405!Secure
```

این فقط نمونه است؛ رمز دیگری انتخاب کنید.

نام کاربری مدیر به‌صورت پیش‌فرض:

```text
admin
```

## مرحله پنجم: ساخت و انتشار

1. پلن دارای Persistent Disk را تأیید کنید.
2. روی Apply یا Deploy Blueprint بزنید.
3. چند دقیقه منتظر بمانید تا وضعیت سرویس `Live` شود.
4. Render یک آدرس HTTPS مانند زیر می‌دهد:

```text
https://novin-production.onrender.com
```

این آدرس را در کامپیوتر و موبایل باز کنید.

## ایجاد کاربر برای کارکنان

1. با حساب مدیر وارد شوید.
2. به بخش «کاربران» بروید.
3. برای هر کارمند نام کاربری و رمز جداگانه بسازید.
4. نقش کارمند را انتخاب کنید.

کارمند می‌تواند تولید ثبت و گزارش‌ها را مشاهده کند، ولی مدیریت کالا، کاربران و پشتیبان فقط برای مدیر است.

## پشتیبان‌گیری

حداقل هفته‌ای یک‌بار:

1. با مدیر وارد شوید.
2. بخش «پشتیبان» را باز کنید.
3. «دانلود پشتیبان JSON» را بزنید.
4. فایل را روی یک کامپیوتر دیگر یا حافظه امن نگهداری کنید.

## اتصال دامنه اختصاصی

پس از انتشار می‌توانید از بخش Settings سرویس Render گزینه Custom Domains را انتخاب و دامنه شرکت را متصل کنید. بدون دامنه نیز آدرس HTTPS خود Render قابل استفاده است.

## نکات امنیتی

- رمز مدیر را برای کارمندان ارسال نکنید.
- برای هر کارمند حساب جداگانه بسازید.
- مخزن GitHub را Private نگه دارید.
- دیسک دائمی را حذف نکنید.
- فایل‌های پشتیبان را در محل امن نگه دارید.
- سایت به‌صورت پیش‌فرض پس از ۱۰ تلاش ناموفق ورود از یک IP، ده دقیقه ورود را محدود می‌کند.
